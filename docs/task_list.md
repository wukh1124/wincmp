## Database
1. [x] 在 資料庫瀏覽 -> Database -> DB Explorer 中, 如果 Databases 內沒有任何表格, 會出現黑畫面當機(要關掉重啟wincmp才可以正常使用)

## 專案管理面板
1. [x] 操作 -> 開啟專案終端 -> 如果是larvael 專案, 需自動返回正確根目錄, 目前出現的問題是打開laravel專案終端後會預設進入了 \public 目錄

## Logs 控制台
1. [x] 有關 Logs 控制台, 每次從 終端日誌(Terminal Logs) 分頁 切換到其他分頁時, Logs 記錄都被清空, 在同一啟動情況下 Logs 需要保留
2. [x] 目前 Runtime (Node/Bun) 在不同專案的 logs 都會打印在同一個 log tab下, 需要為如果有多個 Runtime 同時運行在 Runtime (Node/Bun) 要可支援下拉選單, 用戶可選擇可不同專案的 log, 而不是所有專案的log都混在一起; 另外偵測到有最新的 log打印, 要自動切換到 Runtime 下那個有最新log的下拉選擇專案項目, 方便用戶知道那個專案有新的log打印, 切換時需要有共用的防抖動
3. [x] Log Terminal 打印出來的 Logs 可支援選中和複製嗎?
4. [x] 在 收起/打開 Logs 控制台 這行的最右 加上箭頭按鈕, 功能一樣是收起和打開 Logs 控制台

## UIUX
1. [x] 限制Web介面的視窗最小尺寸, 因為視窗被縮得太小會導致UI擠在一起很難看, 限制視窗最小尺寸可以避免用戶不小心操作
2. [x] 有關 select box的UI, 在本地化語言與日誌設定 -> 顯示語言, 預設專案終端, 新增/修改專案設定 -> 專案框架/類型, 執行器, PHP 執行版本, Runtime 的運行模式, 選擇內建版本的 select box(如有其他select box都一樣處理) 需要把下拉選擇欄右方的箭頭圖示移左一點, 目前太貼近右邊了, 導致右邊看起來失去了 padding, 左右不對稱.
3. [x] 有關 Runtime 運行細節設定 -> 使用 WinCMP 內建執行檔(Bundled Runtime) 需檢測 ./bin/ 內是否有可用的node或bun 執行檔, 如果沒有可用內建執行檔, 需要預設取消勾選, 並在保存時提示用戶沒有可用內建執行檔
4. [x] 在alert提示中 會顯示 wails.localhost 說 XXX, 這可以修改一下, 目前有點奇怪, 因為用戶不知道 wails 是甚麼, 也不需知道

## 頂部欄
1. [x] 有關 版本號 需要把寫死為 v2.0.0 改為在build時根據目前version顯示, 但version放在那? 給我點建議
2. [x] 搜尋專案或設定 目前未有實作功能, 功能改為只支援搜尋專案, 輸入後搜索專案名稱, 根路徑和開發網域, 點擊搜尋結果會跳到專案頁面, 並把相關專案高亮, 滾動條自動拉到第一個乎合的搜索結果的位置
3. [x] 在頂部 Go 核心已連接的左邊可加上 目前 WinCMP 啟動是否有管理員權限, hover提示如沒有管理員權限可能無法自動修改 Hosts 自訂網域

## 設定 Settings
1. [x] 如果用戶在修改設定後, 在沒有儲存的情況下離開設定頁面, 需提示用戶尚未儲存, 是否確定要離開. 

## i18n
1. [x] 目前顯示語言沒有效果, 需要全面檢查一下UI和log出現的文字, 要根據設定的語言顯示和輸出
2. [x] 待確定的一點 wails 支援即時生效變更語言嗎?
3. [x] 目前很多位置都是中英對照的, 需修改為按設定的語言顯示, 例如 儀表板(Dashboard) 需改為 中文: 儀表板, 英文: Dashboard, 無需中文對照, 因為很浪費UI位置