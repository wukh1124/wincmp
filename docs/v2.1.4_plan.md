# v2.1.4 更新計畫

## 1. 核心與初始化 (Core / Init)
- [x] **修復全新初始化時 Caddy 設定缺失**：解決下載獨立執行檔首次啟動時，`conf/snippets/common.caddy` 未自動從內嵌資源解壓縮產生的問題。

## 2. 終端日誌 (Terminal Logs)
- [x] **修復日誌檔案開啟問題**：修復右鍵選單「開啟檔案」在特定路徑下無法叫起系統預設編輯器的錯誤。
- [x] **分頁標籤資訊增強**：
  - 各分類 Tab 標題維持純粹文字，保持介面簡潔清爽。
  - 各分類 Tab 支援 Hover Tooltip 顯示服務完整名稱與緩衝日誌行數（例如：`Caddy - 共 4 行`）。
- [x] **修復暗色主題 (Carbon) 右鍵選單穿透問題**：解決終端日誌右鍵選單與下拉選單背景透明度過高導致文字重疊、辨識困難之問題，導入實體選單背景色與陰影層次。

## 3. 依賴套件管理 (Dependency Manager)
- [x] **自動檢查更新機制**：
  - 進入依賴管理介面時，自動觸發背景靜默「檢查更新」，同步最新遠端依賴配置與本機環境。
  - 加入防抖/冷卻機制，避免重複頻繁發起請求。

## 4. 資料庫瀏覽器 (Database Explorer UI/UX)
- [x] **頂部佈局緊湊化**：
  - 將 `[MariaDB (MySQL) | Redis (NoSQL)]` 切換器移至頂部主標題列同行置右，縮減縱向空間佔用。
  - 移除顯式 IP 標籤，改為 Hover 於分頁按鈕時以 Tooltip 顯示連線位址與狀態（`127.0.0.1:3306` / `127.0.0.1:6379`）。
- [x] **MariaDB 面板操作就近歸位**：
  - 「重新整理」按鈕移至 `DATABASES` 清單標題列同行置右。
  - 「Open In HeidiSQL」按鈕移至 `TABLES` 清單標題列同行置右。
- [x] **Redis 面板佈局最佳化**：
  - 「搜尋鍵名」搜尋列自標題列移至 `KEYS` 列表內容區頂部。
  - 「重新整理」按鈕移至 `KEYS` 標題列同行置右。
  - 「DB 選擇器 (0~15)」移至右側「鍵值內容」標題列左側。
- [x] **視覺高度對齊**：
  - 統一 MariaDB 與 Redis 左右兩側列表標題列高度為固定 48px (`h-12`)，消除切換分頁時的介面跳動。

## 5. 設定說明優化 (Settings UI/UX)
- [x] **系統運作與自動化行為選項統一風格**：
  - 精簡開關項目標題（「記憶服務運行狀態」、「自動更新 Hosts 網域」、「關閉視窗縮小至系統匣」、「自動檢查版本更新」）。
  - 為所有開關項目補齊下方輔助說明文字，並同步更新前後端 i18n 翻譯字典，維持統一俐落的版面結構。

## 6. 新增 PHP 7.4 支援
- [x] **依賴設定與 SHA-256 校驗**：
  - 於 `conf/dependencies.json` 與 `internal/config/default_conf/dependencies.json` 新增 `php74` (v7.4.33 NTS) 與 `php_redis_74` (v5.3.7 PECL) 來源與校驗值。
- [x] **下載管線與擴充搬移**：
  - 於 `downloader_bridge.go` 補齊 `_74` 標籤映射，解壓時自動將 `php_redis.dll` 安裝至 PHP 7.4 的 `ext/` 目錄。
- [x] **範本與預設配置同步**：
  - 更新 `wincmp.json.example`、`default_conf/wincmp.json` 與打包範本，補齊 `7.4.33` 狀態與預設 3 進程配置（起始埠號 `37400`）。
  - 更新 `conf/snippets/php-upstream.caddy.example` 與打包設定，提供 `(php74)` Upstream 示範。
- [x] **文件與推薦機制串聯**：
  - 更新 `docs/dependencies.md` 依賴表格。
  - 與既有 `laravel-php-mapping.json` 無縫聯動（Laravel 6.x~8.x 自動推薦 PHP 7.4）。